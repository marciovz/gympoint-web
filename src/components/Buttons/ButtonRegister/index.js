import React from 'react';

import { Container, Icon } from './styles';

export default function ButtonSave(props) {
  return (
    <Container {...props}>
      <Icon />
      Cadastrar
    </Container>
  );
}
